import React from "react";
import { Link } from "react-router-dom";

const Home = () => {
  return <div>
    <Link to="/Form">
      <button>form</button>
    </Link>
  </div>
};

export default Home;
